# Modbus RTU tests

There are not autotests. Just sketch executing Master y Slave on single ESP device y run Modbus calls with checking results.

## Required libraries
[StreamBuf](https://github.com/emelianov/StreamBuf)